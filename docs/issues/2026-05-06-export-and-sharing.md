# Export & Sharing Preferences

## Summary

Add default settings for document export.

## Requirements

- [ ] Default export format (PDF / Markdown / plain text)
- [ ] Include annotations in export toggle
- [ ] Default export directory

## Affected Areas

- `Preferences.swift` — export keys
- Export service — read defaults from preferences
