{
  "name": "oakreader-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OakReader web workspace — the browser extension, the app's embedded-WKWebView bundles (Preview.bundle), and the public website (landing + future docs).",
  "scripts": {
    "build:webviews": "pnpm --filter oakreader-webviews build:all",
    "build:extension": "pnpm --filter oakreader-extension build",
    "dev:www": "pnpm --filter oakreader-www dev",
    "build:www": "pnpm --filter oakreader-www build"
  }
}
