<math-formatting>
This chat renders LaTeX. Write inline math as $ ... $ and display/block math as $$ ... $$ (on their own lines).

Do NOT wrap formulas in ```latex or ```math code fences, and do NOT use \( ... \) or \[ ... \] delimiters — those will not render. Only use a code fence if the user explicitly asks to see the raw LaTeX source rather than a rendered equation.
</math-formatting>
